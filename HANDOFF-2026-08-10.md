# Overnight state — 2026-08-10 (live document)

Orchestrator session `peppy-wibbling-moler`. Updated at each milestone through
the night; if this session dies, this file is the handoff. Times are Paris
(box switched from UTC at ~22:35 last night).

## Tom's overnight orders (~01:30)

1. Launch the 3 landing-version builders. **H1 = "Builders talk. Your agent
   listens."** (Tom's edit of kit #7); "You don't have time to watch it all.
   Your agent does." used elsewhere on the page.
2. When the slow re-embed drains: **index the top-2026 talks first** (more
   important than the recent-videos backlog still parked for 04:30).
3. Keep enhancing the dashboard: wrapping issues, nothing out of place, and
   the OCR-matches block eats too much vertical space on dense slides — make
   it digestible.
4. Sweep the demo page for leftover agent prose/voice (same cull as the
   dashboard got).
5. Use codex freely tonight (implementation without mid-task steering, review,
   second opinions, web search) — the rate limit resets tomorrow.
6. Maintain this state file. Ship day is tomorrow, together: fresh LXC,
   security audit, tunnel + domain.

## State at 01:30

- Positioning LOCKED: `docs/design/positioning.md` (+ word-law fix: ownership
  attaches to the choice, not the viewing). Kit appended to
  `research/positioning-2026-08-10.md` with Tom's #7 edit. Artifacts: contract
  6be2c1bf…, kit b270f28d….
- Lab prototypes (hero master-control wall / moment cards / corpus grid)
  aligned to the lock, pushed at `4487cb3`. Known nit: moment.html+grid.html
  live in repo-root `public/static/lab/` (path accident), hero in
  `mcp/src/vidtheque_mcp/public/static/lab/`. Consolidate when graduating.
- Re-embed: ~96/156 stages, zero failures, frame-heavy talks last. Slow
  because every item pays the fetch probe + 90s politeness (fixed forward:
  fast-path landed `6dee9db`, applies at next restart).
- Live stack: runs FROM THIS WORKTREE (mcp+worker, full mode, readonly=0),
  serving :8100/:8081. Restart pending at drain applies: migration 0005
  (ask budget), SSE stream, fetch fast-path, decode fan-out, culled dashboard.
- Parked jobs (not_before 04:30): 5 old jobs = 35 recent-video items.
  3 new jobs (`job_ef8735aa0f22`, `job_ac33f421ffed`, `job_5524c2004cd6`) =
  11 failed resubmissions + the top-15 most-viewed 2026 talks
  (scratchpad/top15.txt; vibe reel excluded per Tom).
- Tests: 1003 green at last full run. Everything through `7361476` pushed.

## Overnight plan (decisions pre-made)

- **On drain** (watcher `b98q45v5z` wakes me): restart stack → verify
  migration 0005 + worker log format → un-defer ONLY the 3 new jobs (top-2026
  priority per order #2; old 5 stay at 04:30) → watcher on the batch →
  demo mode stays OFF until the batch finishes (readonly flip masks nothing
  publicly; box isn't exposed).
- **Landing builders** (3 Opus agents, parallel, separate files under
  mcp/src/vidtheque_mcp/public/static/lab/versions/): V1 "master control"
  dark, V2 "paper" screenpipe-register light, V3 "receipt-first". All: kit's
  five beats, H1 per order #1, CTA "Ask the corpus" (canned), attribution
  line, screenshots for morning judgment.
- **Dashboard polish agent** (Opus): width sweep for wrapping/misplacement,
  OCR-density digest on video detail.
- **Codex**: demo-page prose cull (well-specified, no steering needed);
  later a code review of the night's diff if budget allows.
- Push cadence: after each completed agent, when no sibling mid-run.

## Decisions log (append as they happen)

- 01:30 — this file created.
- 01:45 — dispatched: 3 landing builders (v1 master-control, v2 paper,
  v3 receipt-first → lab/versions/), dashboard polish agent (wrap sweep +
  OCR digest), codex gpt-5.6-sol on the demo-page prose cull
  (workspace-write in this tree, scope-fenced to 4 files, orchestrator
  commits after diff inspection; run dir scratchpad/codex-cull/).
- 02:05 — V2 "PAPER" landed (`b8b4584`): H1 per order, second line over the
  dark corpus wall ("does." in acid), annotated specimen from real ocr_lines
  coords, herdr ledger band, field-test terminal beat (4 speakers not 5 —
  accuracy over the kit's number), two documented DESIGN.md divergences
  (mono display H1, hard-offset CTA shadow — lab-only). Not pushed yet
  (siblings mid-run).
- 02:25 — V1 "MASTER CONTROL" landed (`21e8b96`): full-page gallery world,
  second line as the deck under the H1, specimen with all 34 real OCR boxes,
  multiviewer source bar (invented, screenshot-survivable), agent terminal
  with re-verified quotes, MCP first named in beat 5. Not pushed yet.
- 02:40 — V3 "RECEIPT-FIRST" landed (`2243dd8`+`2938cad`): specimen-as-hero
  (PR-on-a-slide frame with real boxes), the corpus's own profanity kept on
  m41 (Tom's call stands open), agent transcript promoted to second hero,
  gold-on-black with SEEN-only lime. All three versions now home.
- 02:55 — codex cull landed and committed after inspection: demo page
  meta/tagline now speak the lock, teaching prose gone, MCP demoted to a
  labeled endpoint, 1003 tests green. ONE orchestrator override: codex culled
  the footer's "the videos belong to the people who made them" — restored
  (attribution is a positioning obligation, now pinned in demo-site.md §3.7
  as never-cullable).
- 03:05 — RE-EMBED COMPLETE: 156/156 stages, zero failures; the whole corpus
  is on Qwen3-VL-Embedding-2B at 2048/bf16. Stack restarted: migration 0005
  applied (ask_budget table live), SSE stream + fetch fast-path + decode
  fan-out + culled surfaces now serving. Top-2026 jobs un-deferred (3 jobs:
  15 top talks + 11 failed resubs); 5 backlog jobs stay parked for 04:30.
  Watcher on all 8 to full drain. Note: readonly stays 0 until the batch
  finishes; demo mode restored at final drain.
- 03:40 — dashboard polish landed (`4acfdd8`..`3d67812`): 11 wrap fixes
  each pinned to the width that showed it, the OCR digest primitive (8-line
  preview = exactly the :has() pair count, honest +N expander, box-linkage
  scoped so expanded lines can't light wrong boxes), event log same primitive,
  1011 tests. ALL overnight agents now complete; everything pushed through
  `3d67812`. Codex night-end security review dispatched over ff239de..HEAD
  (read-only, findings for the morning audit).
- 04:00 — codex review returned: 0 CRITICAL, 1 HIGH (stream disconnect
  refunds paid upstream work — attacker-drainable budget accounting),
  3 MEDIUM (trusted-CIDR/tunnel-proxy footgun; past-end hides pool
  exhaustion; fast-path resolves bare 11-char ids without host allowlist —
  wrong-video tagging), 3 LOW. Full text:
  scratchpad/codex-review/final.md. Fix agent dispatched for all seven
  (CIDR item gets boot-warning + runbook guard tonight; deeper redesign
  deferred to the morning audit).
- 02:40 (real Paris time — earlier ledger stamps ran ~1h fast, from a
  UTC-epoch process) — all seven codex findings FIXED and pushed
  (`58c319e`..`3034176`, 1021 tests): Billing.paid gates refunds (HIGH),
  host-allowlisted fast path (also closed the same hole in index-video's
  already-indexed check), pool honesty, lab esc(), runbook, CIDR boot
  warning. NOTE: live stack still runs pre-fix code; the final drain restart
  applies everything. Deferred to morning audit (in the agent's words, all
  documented in-repo): the real CIDR/tunnel fix needs Tom; Billing timeout
  accounting choice; lab href encoding; _empty_result payload shape.
- 06:45 — NIGHT COMPLETE. Batches drained: corpus at **123 ready videos**
  (+45 tonight incl. the top-2026 talks). 10 overnight E_RATE_LIMIT items +
  1 E_NOT_READY_YET (unpublished premiere) → resubmit in daylight. Stack
  flipped to demo mode on the fully-fixed code; smoke test green: the
  flagship OCR query returns its slide with match_start in the payload.
  Everything pushed through `8c7624e`. The box is quiet.

- ~13:30 (daylight session) — Tom ordered the NEXT top-2026 batch. Rebuilt the
  ranking from channel.json (2026 boundary binary-searched: positions 0..420,
  last 2026 video 2026-01-05): the old batch had skipped not just the vibe reel
  but the 3 workshop-format videos (96/112/75 min, incl. the channel's actual
  #1, Matt Pocock's 1.3M-view walkthrough). Tom's call: talks only, workshops
  stay out. Next 15 talks (83k→43k views, all ≤44 min, list in session
  scratchpad next15.txt) submitted as `job_6eb1a790e135` (10) +
  `job_bc6f5b653038` (5). Stack restarted with READONLY=0 for the submission
  (stack.env flipped; restore to 1 + restart at drain — box still not exposed).
  Watcher `brkkxmast` on both jobs. The 10 E_RATE_LIMIT + 1 premiere resubs
  still pending — deliberately NOT mixed into this batch to keep bot-check
  pressure down; resubmit after it drains.

- 15:20 — next-15 batch drained: 9/15 done (corpus at 132 ready), 6/15
  E_RATE_LIMIT — everything fetched after ~14:05 hit the bot-check, incl. all
  of job_bc6f5b653038. Tom (away, via chat): keep going — next tranche of
  top-2026 talks once this batch is done. Not submitting into the active
  block; probe watcher `b7o74ihbe` (1 metadata fetch / 20 min, 2-success
  streak = lifted, 8h cap) will wake the session to build+submit the next 15
  (the 6 failed items will top that ranking — they're not in the DB).
  READONLY stays 0 until that batch drains; restore then.

- 15:45 — bot-check lifted (probe: 2 clean metadata fetches). Tranche 2
  submitted: `job_1df472fa9b38` (10) + `job_9cee0668392d` (5) = the 6
  rate-limited retries (incl. r305-aQTaU0, which had left a failed video row —
  ranking filter now excludes only ready/pending) + next 9 talks by views
  (42k→30k). Watcher `b7ubjt924` to drain; then restore READONLY=1 + restart.
  Old-backlog resubs (10 E_RATE_LIMIT + 1 premiere + pending B8l81jhvHbI)
  still deliberately separate.

- 16:55 — tranche 2 drained 15/15 (all 6 retries incl.), corpus 147 ready.
  Briefly restored demo mode, then Tom (mid-turn): "Continue extracting the
  top videos rather than restore the demo" — READONLY back to 0, stack
  restarted, ROLLING TRANCHES now the standing order: each drain wakes the
  session, which submits the next 15 top-2026 talks (same filter) until Tom
  says stop. Tranche 3 submitted (29k→19k views): `job_0b6970629fb2` (10) +
  `job_9f2ddc5d7e75` (5), watcher `bexdchoje`. Demo mode NOT restored —
  restore only when Tom ends the rolling order.

## Open items for Tom (morning)

- Judge the 3 landing versions (screenshots + dev server).
- `slop` chip rank-1 quote contains profanity — kept as-is pending your call.
- Organiser-consent email (gates public demo copy per kit stress-test §).
- Ship day: fresh LXC, audit together, tunnel + domain. Stack should move
  back to the main checkout (pull + `make sync-gpu` from a real terminal).
