# Overnight handoff — 2026-08-09, morning

Tom: the short version is **the corpus is real, the site is at its ship gate,
and everything is pushed.** Try the demo at `http://127.0.0.1:8100/` (the live
stack was flipped to public read-only mode after the batch finished — flip
notes below). Nothing was published externally; tunnel + domain wait for us
together, and the security audit stays deferred per your call.

## The corpus

**57 videos · 20.4 hours · 12,895 transcript cues · 2,074 chunks ·
3,460 keyframes · 41,138 OCR lines · 886 MB.** 54 of 57 transcribed with
whisperX large-v3 (the other three predate the language fix or came in via
captions). All AI Engineer, latest-first, ≤75 min enforced.

Batch story: 116 queued, 55 indexed fresh overnight + the 2 from yesterday.
YouTube's bot-check ("Sign in to confirm you're not a bot") appeared at wave 2
misclassified as a permanent failure — fixed live (it's throttling; now
rate-limit-family with defer/backoff), driver hardened (item-level cool-off,
E_INDEXING patience, full retry pool), and politeness raised at 05:10 after a
7/10-throttled wave (2s/req, 30–60s sleeps, 90s between videos) — after which
**waves 5 and 6 went 20/20 clean**. Sixteen videos remain in the throttle
pool; two more (`O-CBZ3JtRvo`, `GgLQ02aO-hs`) show `fetch failed` and answer
`data_status: degraded` honestly.

**To index the stragglers** (flip readonly off first, see below):
`uv run --no-sync scripts/mcp_call.py call index-video '{"urls": [...16 ids...]}'`
— they're the difference between `batch_list.tsv` and `list-videos`.

## The site

At its ship gate: Playwright pass (2 blockers fixed), polish pass (empty
state, a11y, CLS 0, XSS-hardened), opencode review (#1–#4 musts fixed and
browser-verified: stale-response race, append-aware errors, budget refunds,
citation seam). Thumbnails are real now (`?w=` honored through a byte-capped
derived cache; results pages ~50–100 KB, was 886 KB). Ask mode runs
deepseek-v4-flash-0731 via your OpenRouter key with per-IP + global daily
budgets and refund-on-failure.

**Mode flips** (in `/home/dev/vidtheque-data/stack.env`, then
`DATA_DIR=/home/dev/vidtheque-data scripts/dev_stack.sh stop && … start`):
`VIDTHEQUE_PUBLIC_READONLY=1` = demo mode (write tools masked, page at `/`);
`0` = full MCP for indexing. The politeness overrides from 05:10 are still in
stack.env — keep them for any daytime indexing.

## The night's ledger

- **653 → 700+ tests** across the suites (336 at midnight). ~35 commits, all
  pushed through the night; history is clean (one forced update, documented in
  CLAUDE.md's new fix-forward rule).
- **External reviews, all actioned**: 3 codex (jobs, queries, pipeline — 28
  findings) + 2 opencode (frontend/facade, worker — 16+ findings). Every
  finding fixed with a regression test, deferred with rationale, or declined
  in writing. Research docs carry the ledgers.
- **Sonnet MCP-design bench**: 12 findings (research/mcp-design-bench-2026-08-09.md),
  prose fixes shipped, F1–F12 structural items deferred. Best defect found:
  a truncation marker advising a parameter the tool doesn't have.
- **Incidents survived**: the rickroll (cleaned via cascade), instance-blind
  stop script (fixed twice, now listener-scoped), the bot-check saga, and a
  git divergence from pushing under a mid-run agent (rule added).

## Backlog (rough priority)

1. Wire the worker's new typed 400s (`invalid_input`/`invalid_image`/
   `invalid_media`) into pipeline classification: skip-frame not fail-stage.
2. Frame-level OCR FTS (task #20 — multi-term AND can't match across lines).
3. F1–F12 from the design bench (structured OCR, per-call-site markers, …).
4. Retry the 16 pooled videos + re-fetch the 2 degraded ones.
5. Small: channel-count display quirk in corpus-summary; DOM-level test
  harness for the page (prototype in scratchpad); demo/polish snapshot data
  dirs (`~/vidtheque-demo-data`, `~/vidtheque-polish-data`) can be deleted.

## Reserved for together

Cloudflare tunnel + domain purchase + DNS, and the deferred security audit,
before anything goes public. The read-only mode + rate limits are ready for
that day; nothing is exposed today.
