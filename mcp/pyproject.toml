[project]
name = "vidtheque-mcp"
version = "0.0.1"
description = "vidtheque MCP server (placeholder — framework selection pending)"
readme = "NOTE.md"
requires-python = ">=3.12,<3.13"
license = { text = "MIT" }
# Intentionally empty: the MCP framework is being chosen by a parallel research
# task, and picking one here would prejudge it. See NOTE.md.
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vidtheque_mcp"]
