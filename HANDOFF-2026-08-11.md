# Release-day state — 2026-08-11 (live document)

Orchestrator session `peppy-wibbling-moler` (same session as 08-10). Updated at
each milestone through the night; if this session dies, this file is the
handoff. Times are Paris. **Today is release day: infra staged overnight,
launch together in the morning after Tom's security branch merges.**

## Tom's overnight orders (~01:30, before bed)

1. Index: finish top-2026 to the 10k floor, then MOST-RECENT phase — newest
   talks (same workshop exclusions) + the parked backlog folded in. Use the
   new rate-limit system (circuit breaker + no-attempt-burn accounting).
2. Release scope: **infra staged, launch together** — research and stage the
   fresh-LXC plan, tunnel config, domain checklist, migration runbook; nothing
   goes public overnight. Research with opus + codex (codex through herdr).
3. Another MCP eval round with **sonnet AND terra** consumers; keep enhancing
   the surface.
4. UI: iterate freely on everything SHIPPING-related; NO UI work Tom didn't
   ask for; finish what was started today.
5. Morning package: a few UI fixes visible + a before-ship todo list.
6. Tom works security fixes in ANOTHER WORKTREE tonight; I keep pushing main;
   his branch merges tomorrow morning.
7. Demo mode not needed overnight (flipped off ~01:30 for indexing).

## State at 01:45

- Corpus: **190 ready**. Tranche 7 (`job_263a6bec5cf7`, 9 items incl. the 6
  burned-under-old-accounting retries) submitted — THE LAST most-viewed
  tranche; **10k floor reached**, most-recent phase next (builder ready:
  scratchpad build_recent15.py, backlog list inlined). Watcher `boon7go6x`.
- Live stack: THIS worktree, readonly=0, all of today's code (dashboard r4
  incl. F5 favicon, demo/ask fixes through `a809131`, yt-dlp circuit breaker +
  consolidated extraction, terra-r2 surface fixes, recalibrated floors).
- Pushed through `70d10cd`. Suite 1121 green at last agent run.
- Prod backup (rogue-agent insurance): `/home/dev/backups/vidtheque-2026-08-11/`
  — DB via online backup API (integrity ok, 181 ready at snapshot), all data
  files, write-protected chmod a-w.
- In flight: demo r4 agent (width economy, thumb consistency, stutter video
  via playwright, refusal-content removal, F5 favicon swap in index.html).
- Identity: F5 "the v." favicon CHOSEN (contracts updated `fceb6a2`;
  dashboard template swapped `94af91d`; demo swap with the r4 agent).
- Keyframes+OCR merge shipped REVERSIBLY: `git revert 1ed6516` restores the
  split (docs in the separate `70d10cd` §12.7).

## Overnight plan

- On tranche-7 drain: submit most-recent tranches back-to-back (15/batch,
  probe-gate on bot-check waves), rolling until morning or list exhaustion.
- Release-prep research (dispatching ~01:50): opus architecture/staging agent
  + codex-in-herdr second opinion → `research/release-staging-2026-08-11.md`
  + the BEFORE-SHIP todo (deliverable for Tom's morning).
- Eval round 3 (dispatching ~01:50): opus orchestrator, sonnet + terra
  consumer fleets on the post-fix surface → append to the terra eval doc +
  fix agent on whatever it finds (shipping-related fixes only).
- Morning package: UI-fix summary with screenshots + before-ship list, in this
  file + artifacts.

## Decisions log (append as they happen)

- 01:45 — this file created. Tranche 7 running. Demo r4 in flight.
- 02:20 — DEMO R4 LANDED + pushed (through `15239d7`, 1133 tests): ask pane
  673px→full `--maxw` column with a registered `--bp-wide` two-column layout
  (answer 70ch + Sources beside); ONE thumb geometry for all result kinds
  (root cause: ocr+frame wasn't "frame"); stutter measured with a LayoutShift
  observer — the growing work log was the main culprit, now a fixed 6-line
  box; CLS 0.409→0.215, 8 shifts→2 (video: scratchpad review/demo-stutter.webm,
  BEFORE/AFTER side by side, for Tom); refusal chip + semantic-legs note gone
  from the demo route group only (tool still says it; test guards the seam);
  F5 favicon byte-identical across both surfaces. Morning UI package: the
  demo-r4-* screenshot set + the webm. Both r4 rounds now complete.

- 02:50 — EVAL ROUND 3 LANDED + pushed (through `a054c94`, 1137 tests).
  THE finding (14.1 HIGH): a conformant MCP client may render
  structuredContent INSTEAD of the text block — Claude Code does — so every
  prose-only `note:` (clamps, reconciliation, degradation reasons) was
  invisible to a whole client class; video-summary returned four content-free
  keys to them. FIXED: parity mirroring. Also fixed: zero-results now carries
  a `reason` key (4 causes distinguishable), tags= silent no-op, speaker=
  dead-end now names video_title=, empty unreachable parenthetical. Deferred:
  two LOWs (guide clause, harness facts). Both fleets answered round-2 §12:
  the title note WORKS. Demo doc §13: new paraphrase pair (LLM-as-judge
  disagreement, zero shared content words). CAVEAT: parity fix pinned by 7
  tests but UNVERIFIED live — re-run the sonnet smoke probe after the next
  restart (tranche 7 at 7/9, restart at its drain).

- 03:20 — RELEASE STAGING LANDED + pushed (`2e7e15d` research 1,636 lines,
  `d718236` BEFORE-SHIP.md 660 lines). Topology recommendation: fresh
  unprivileged CPU-only LXC for mcp/ ONLY; worker stays on the GPU box over
  the host bridge (HTTP boundary = the designed shape; deletes the GPU
  passthrough variance from launch morning). Codex review: 25 findings folded,
  3 blockers verified real — systemd env would have 502'd the tunnel AND
  LAN-exposed the origin; dev_stack.sh cannot express the split topology;
  /mcp unlimited + AUTH=none = anonymous GPU starvation. NEW SHIP GATES
  surfaced: /static/lab publicly served (13MB of unreleased prototypes!),
  no takedown path despite the positioning promise, corpus-frozen
  verification. Ship-docs agent dispatched on the agent-doable list
  (lab gating, SECURITY.md via GitHub advisories, README footgun,
  compose stub, worker README topology, crawl math, takedown runbook).

- 03:50 — MOST-VIEWED PHASE COMPLETE: tranche 7 9/9 (the six burned items all
  recovered under the breaker), corpus **199 ready**. Stack restarted onto the
  parity fix; sonnet smoke VERIFIED live (video-summary structured carries
  full content; zero-results carry `reason` + mirrored notes) — eval-r3
  caveat closed. MOST-RECENT PHASE OPEN: tranche 1 = 9 backlog resubs (incl.
  the premiere, and one workshop that rides because it was in the original
  parked backlog) + 6 fresh recent talks (jobs fda4/44f3, watcher chains next
  tranches at each drain). Ship-docs agent still running.

- 04:30 — SHIP-DOCS LANDED + pushed (through `e522483`, 1139 tests). All 7:
  /static/lab GATED on the public route (prefix denial on resolved paths,
  test red-then-green); SECURITY.md (GitHub private vuln reporting — Tom must
  ENABLE it in repo settings); compose stub + README quickstart footgun fixed
  (public path now uses the overlay); worker/README on the unified topology;
  crawl math recomputed live (12,351 keyframes, ~1.6GB vs 256MB cache cap —
  6.4x over, real bound is the frames limiter); docs/takedown.md REHEARSED on
  a DB copy (plain sqlite3 cannot cascade — vec0 triggers + FK pragma; 1.78s
  per talk measured; footer now says "Removal on request"). BONUS: fixed
  index-schema §3.3 drift query BEFORE-SHIP 3.9 relied on (would have failed
  on launch morning — dup keyframes are never embedded). BEFORE-SHIP updated.
  NIGHT WORK COMPLETE except the rolling recent-phase tranches.
  Tom's two blanks: takedown contact + response time (takedown.md §1);
  enable private vuln reporting.

- 07:20 — RECENT PHASE, FOUR TRANCHES CLEAN: 15/15 x4 (backlog + premiere all
  in), corpus **259 ready**, ZERO failures since the circuit breaker went
  live (~60 videos overnight at ~15/65min). Tranche 5 running (jobs
  3cad/a680). Remaining unindexed 2026 talks: roughly two tranches' worth.

## Open items for Tom (morning)

- Merge your security branch; joint audit remainders from 08-10 (real
  CIDR/tunnel fix, Billing timeout accounting, lab href encoding,
  _empty_result shape).
- Judge: keyframes+OCR merge (revert command above), demo r4 results,
  the before-ship todo.
- Organiser-consent email still gates public demo copy (kit stress-test §).
- Launch: fresh LXC + tunnel + domain, together, per staged runbook.
